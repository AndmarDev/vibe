{
  "name": "@app/test-hub",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@app/model": "workspace:*",
    "@app/rules": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
